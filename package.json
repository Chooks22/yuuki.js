{
  "name": "yuuki.js",
  "version": "0.0.0",
  "description": "Next generation discord bot framework",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "turbo run --filter=!yuuki-bot-* dev",
    "build": "turbo run --filter=!yuuki-bot-* build",
    "lint": "turbo run --filter=!yuuki-bot-* lint"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "turbo": "^1.10.16"
  }
}
